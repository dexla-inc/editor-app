import { Text } from "@mantine/core";

type Props = {};

export default function PageActions({}: Props) {
  return (
    <>
      <Text size="sm">
        Lets load triggers and actions here using the exact same component.
      </Text>
      <Text size="sm">
        The triggers are the different part here which relate to a page loading.
      </Text>
    </>
  );
}
