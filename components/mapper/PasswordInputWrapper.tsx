import { PasswordInputProps } from "@mantine/core";

type Props = PasswordInputProps & {
  children: any;
  isPreviewMode: boolean;
};

export const PasswordInput = ({ children }: Props) => {
  return;
};
