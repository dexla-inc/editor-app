import { AvatarData as Avatar } from "@/components/data/AvatarData";
import { SelectData as Select } from "@/components/data/SelectData";
import { TextData as Text } from "@/components/data/TextData";
import {
  TitleData as Badge,
  TitleData as Button,
  TitleData as FileButton,
  TitleData as Link,
  TitleData as NavLink,
  TitleData as Title,
} from "@/components/data/TitleData";

export const dataMapper = {
  Select,
  Text,
  Title,
  Link,
  Button,
  Avatar,
  NavLink,
  Badge,
  FileButton,
};
